# 제안서 — 번역 방안 & 편의성 개선

작성: 2026-06-05 · 대상: 이미지→워드 리포트 도구

---

## 1. 번역: 영구 무료 · 가벼움 · (가능하면) 서버 없이

### 결론
**MyMemory API를 1순위로 권장합니다.** 키·서버·결제수단 없이 브라우저에서 바로 호출되며 영구 무료입니다.
지금 막혀 있던 "DeepL 키 발급 + Vercel 서버" 부담이 **통째로 사라집니다.**

### 비교

| 방식 | 무료 지속 | 키 필요 | 서버 필요 | 한·일→영 품질 | 한도 | 비고 |
|---|---|---|---|---|---|---|
| **MyMemory** ⭐ | 영구 | ❌ | ❌(CORS 직접) | 보통~양호 | 익명 5,000자/일, 이메일 등록 50,000자/일 | 요청당 500바이트 → 문장 단위 분할 필요 |
| Transformers.js (NLLB-200 distilled, 브라우저) | 영구 | ❌ | ❌ | 양호 | 무제한(오프라인) | 최초 모델 **242MB 다운로드**, 문장당 2~5초 |
| DeepL API Free | 영구 | ✅ | ✅(키 숨김) | 최상 | 50만자/월 | 가입 시 카드 등록 |
| Google Cloud Translation | 영구 | ✅ | ✅ | 양호 | 50만자/월 + 무료 글로서리 | GCP 셋업 필요 |
| LibreTranslate(자가호스팅) | 영구 | ❌ | ✅(직접 운영) | 보통 | 무제한 | 모델 수 GB, GPU 권장 |

### 권장안: MyMemory + 로컬 용어집(후처리)
MyMemory의 약점은 **전문용어**입니다. 이를 **돈 안 들이고** 보완하는 방법:
1. MyMemory로 1차 번역(브라우저에서 직접, 서버 X)
2. **사용자 용어집**(한/일 → 영 매핑, 로컬 저장)으로 결과를 후처리 치환 — 예: `계선주→bollard`, `라싱→lashing`
3. 하단 영문 칸에서 최종 수정(이미 구현된 편집 UI)

→ 유료 글로서리(DeepL Starter) 없이도 **용어 일관성**을 확보. 용어집은 IndexedDB에 보관(이미 도입한 저장소 재사용).

### 대안(품질 최우선·오프라인 필요 시)
- 인터넷/외부 전송이 꺼려지면 **Transformers.js(NLLB-200)** — 완전 오프라인, 단 242MB 다운로드 + 느림.
- 번역 품질을 최고로 올리려면 **DeepL Free**(키+서버) 유지.

> 권장 진행: **MyMemory로 전환(키·서버 제거)** → 필요 시 용어집 후처리 추가. DeepL 코드는 옵션으로 남겨둠.

---

## 2. 편의성 개선 아이디어 (효과/공수)

| # | 아이디어 | 효과 | 공수 | 추천 |
|---|---|---|---|---|
| A | **로컬 용어집** (한/일→영 자동 치환) | 높음 | 소~중 | ★ |
| B | **자동 번역**(입력 멈추면 0.7초 후 자동) | 중 | 소 | ★ |
| C | **Date 입력 필드**(모달에서 날짜 → 셀의 ‘Date of Rectified’ 자동 기입) | 중 | 소 | ★ |
| D | **생성 전 미리보기**(브라우저에서 docx 렌더 확인 후 다운로드) | 높음 | 중 | ★ |
| E | **After 사진 슬롯**(수정 후 사진까지 한 번에 삽입) | 중 | 중 | |
| F | **드래그로 섹션 순서 변경** / 선택 순서 지정 | 중 | 중 | |
| G | **키보드 확장**(Shift 범위선택, A 전체선택, 숫자 점프) | 중 | 소 | ★ |
| H | **중복 이미지 감지**(같은 사진 경고) | 낮음 | 소 | |
| I | **간단 이미지 편집**(회전/크롭) | 중 | 중 | |
| J | **프로젝트 다중 저장**(작업 여러 건 분리 보관) | 중 | 중 | |
| K | **PWA 설치**(오프라인 앱처럼 실행) | 낮음 | 소 | |
| L | **저장 상태 표시**("자동 저장됨" 인디케이터) | 낮음 | 소 | ★ |

### 1차 추천 묶음 (가성비)
**B(자동번역) + C(날짜 입력) + D(생성 전 미리보기) + G(키보드 확장) + L(저장표시)** — 공수 작고 체감 큼.
용어집 A는 번역 방식 확정 후 함께.

---

## 3. 점검 결과 (소소한 개선거리)

- 모달 번역 안내문 일부가 한국어 고정 → i18n 사전으로 편입 필요(일본어 사용자 대비)
- 생성 실패 alert 문구도 i18n 적용 여지
- IndexedDB 저장 한도 초과 시 사용자 안내 메시지(현재는 조용히 건너뜀)
- favicon 404(브라우저 자동 요청) — 파비콘 1개 추가로 제거 가능(기능 무관)
- 상단 letterhead + "No / Observation·Requisition items" 헤더행 → **원본 docs 입수 후 1회 반영**(대기 중)

---

## 4. 대기 항목
- **원본 docs 양식**(또는 상단 헤더부만이라도) 입수 시: letterhead·헤더행 반영, 정확한 셀 폭/테두리 두께 최종 일치.
- 그전까지 섹션 셀은 첨부 이미지 기준으로 완성된 상태 유지.

---

출처: [MyMemory 사양](https://mymemory.translated.net/doc/spec.php) · [MyMemory 사용한도](https://mymemory.translated.net/doc/usagelimits.php) · [브라우저 직접 호출 예시](https://lilting.ch/en/articles/mymemory-api) · [LibreTranslate](https://github.com/LibreTranslate/LibreTranslate) · [NLLB-200 브라우저 번역](https://alexop.dev/posts/building-client-side-ai-translator-vue/)
