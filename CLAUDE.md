# CLAUDE.md

이 프로젝트에서 작업할 때 반드시 지킬 규칙.

## 프로젝트 한 줄 요약

다량의 이미지(평균 300장)를 브라우저에서 업로드 → 키보드로 필요한 것만 선택 →
워드 템플릿(.docx)의 특정 셀에 일정한 포맷으로 삽입 → 완성본 다운로드.
**서버 없음. 모든 처리는 브라우저(클라이언트)에서 한다.**

## 핵심 원칙

### 1. 오버엔지니어링 금지
- 지금 필요한 것만 만든다. "나중에 필요할 수도"는 만들지 않는다.
- 추상화 레이어, 디자인 패턴, 상태관리 라이브러리를 "혹시 몰라서" 추가하지 않는다.
  상태는 기본(React state/context)으로 시작하고, 정말 막힐 때만 도구를 추가한다.
- 백엔드/DB/인증을 임의로 추가하지 않는다. 요구사항은 정적 사이트로 끝난다.
- 의존성은 최소로. 새 패키지를 넣기 전에 표준 웹 API(Canvas, File API 등)로
  되는지 먼저 확인한다.
- 설정 옵션을 미리 늘리지 않는다. 값은 상수로 두고, 요구가 생기면 그때 옵션화한다.

### 2. 이미지 최적화 (필수)
- **갤러리용 썸네일과 삽입용 원본을 분리한다.** 갤러리에는 작은 썸네일
  (긴 변 ~256px)만 그려서 300장도 가볍게 띄운다.
- 300장 전부를 메모리에 풀 해상도로 들고 있지 않는다. `File` 참조만 유지하고,
  필요한 순간에만 디코딩한다. `URL.createObjectURL`로 만든 URL은 쓰고 나면
  반드시 `revokeObjectURL`로 해제한다.
- 갤러리 그리드는 가상 스크롤(또는 lazy 디코딩)로 보이는 것만 렌더한다.
- **삽입 전 이미지는 다운스케일 + 압축한다.** 셀에 들어갈 실제 픽셀 크기 이상으로
  큰 원본을 그대로 박지 않는다. 긴 변 기준 상한(예: 1600px)으로 리사이즈 후 JPEG
  압축(품질 ~0.8)으로 .docx 용량을 잡는다.
- EXIF 회전 정보를 반영해 사진이 돌아가지 않게 한다.
- 워드 호환을 위해 삽입 포맷은 **JPEG/PNG**를 기본으로 한다(WebP는 구버전 워드에서
  깨질 수 있으므로 변환해서 넣는다).

### 3. 키보드 우선 UX
- 갤러리 선택은 방향키(←→↑↓)로 커서 이동, **스페이스**로 선택/해제.
- 마우스 없이 선택→생성까지 가능해야 한다. 단축키를 임의로 늘리지 않는다.

## 스택 (확정)

- 빌드/런타임: **Vite + React + TypeScript**
- 워드 생성: **docxtemplater + pizzip** (+ 이미지 모듈), 전부 브라우저에서 실행
- 이미지 처리: Canvas 기반 리사이즈/압축 (필요 시 `browser-image-compression`)
- 다운로드: Blob + `a[download]` (또는 file-saver)
- 배포: **Vercel 정적 호스팅** (서버리스 함수도 사용하지 않음)

## 하지 말 것 (요약)
- 서버/DB/로그인 추가 ❌
- 안 쓰는 추상화·패턴·전역 상태 라이브러리 ❌
- 미리 만드는 설정 화면·플러그인 구조 ❌
- 원본 이미지를 최적화 없이 .docx에 그대로 삽입 ❌
