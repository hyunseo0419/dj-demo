# 기능명세서 (초안)

> 워드 양식 파일이 아직 없어 일부는 가정으로 작성. 양식 첨부 후 §6(템플릿 연동) 확정.

## 0. 목표
다량의 이미지 중 유저가 필요한 것만 선택하여, 워드 템플릿(.docx)의 특정 셀에
일정한 포맷으로 삽입한 완성본을 다운로드한다. 전 과정 브라우저에서 처리(서버 없음).

## 1. 사용자 플로우
1. **업로드** — 이미지 여러 장(평균 300장) 선택/드래그로 업로드
2. **선택** — 갤러리에서 키보드로 필요한 이미지만 선택
3. **템플릿** — 워드 템플릿(.docx) 업로드 (또는 기본 내장 템플릿)
4. **생성** — 선택 이미지를 템플릿 특정 셀에 삽입
5. **다운로드** — 완성된 .docx 즉시 다운로드

## 2. 이미지 업로드 (F1)
- `<input type="file" multiple accept="image/*">` + 드래그앤드롭
- 지원: JPEG, PNG (그 외 형식은 무시하고 알림)
- 업로드 직후 각 파일의 **썸네일(긴 변 ~256px)** 생성, 원본은 `File` 참조만 유지
- 300장 기준 프리징 없이 점진적 로딩(썸네일 비동기 생성 + 가상 스크롤)

## 3. 이미지 선택 — 키보드 (F2)
- 갤러리는 그리드. 한 칸에 **포커스 커서** 표시
- **←→↑↓**: 커서 이동 (그리드 경계 처리)
- **Space**: 포커스된 이미지 선택/해제 토글
- 선택된 이미지: 체크 표시 + **삽입 순서 번호** 노출
- 삽입 순서 = 그리드 순서(좌상단→우하단). *(결정 필요: §7-3)*
- 보조(선택): `Enter`=다음 단계, `Esc`=전체 해제. 그 외 단축키 추가 금지
- 하단에 선택 개수 / 전체 개수 표시

## 4. 이미지 최적화 (F3) — 삽입 직전 처리
- 선택된 이미지만 대상으로 처리(전체 X)
- 리사이즈: 긴 변 상한 1600px (상수, 추후 양식에 맞춰 조정)
- 압축: JPEG 품질 0.8
- EXIF orientation 반영
- 결과는 셀 삽입용 바이너리로만 사용, 화면에 다시 그리지 않음

## 5. 워드 양식 구조 (실제 파일 분석 결과)
파일: `1. MONDIAL COSMOS ... Observation and Rectified report.docx`
- ⚠️ 첨부 파일은 70여 개가 채워진 **샘플**일 뿐. 실제 기준은
  **빈 섹션 1개가 기본 단위**이고, 선택 이미지 수만큼 빈 섹션이 반복 생성된다.
- 문서 = 헤더 표 + **섹션 표 N개** 의 나열. 섹션 1개 = 표 1개(5행).
- 섹션 표 한 칸 구성:
  - R0: (좌측 좁은칸) **섹션 번호** / (넓은칸) 관찰 내용 설명 텍스트
  - R1: `Before` / `After` 라벨
  - R2: **Before 셀 = 이미지 삽입 위치** / After 셀(비움, 수정 후 사진용)
  - R3: `Date of Rectified ( yyyy/mm/dd ) :`
  - R4: `Remark:`
- Before 셀 폭 = 4606 dxa ≈ 3.2in(8.1cm). 단일 이미지는 셀 폭 ~8cm로 표시.

## 6. 워드 생성 (F4)
- **이미지 1장 = 섹션 1개.** 선택 이미지 N장 → 섹션 표를 N개 복제 생성.
- 섹션 번호는 1..N 자동 증가. Before 셀에 이미지 1장 삽입.
- After / 설명 / Date / Remark 칸은 **비워둠**(워드에서 유저가 직접 작성).
- 삽입 크기: Before 셀 폭 기준 고정 너비(~8cm), 비율 유지 자동 높이.
- 라이브러리: docxtemplater + pizzip + 이미지 모듈 (브라우저 실행)
- 구현 방식(택1, §7-2):
  - (A) 양식의 섹션 표를 docxtemplater 루프(`{#sections}…{/sections}`)로
    감싸고 Before 셀에 `{%image}`, 번호칸에 `{number}` 태그 삽입 → 가장 코드 적음
  - (B) 섹션 표 1개를 원본으로 OOXML에서 N회 복제(태그 불필요, 코드 더 많음)

## 7. 결정/설정 필요 항목
1. **이미지 모듈**: docxtemplater 공식 이미지 모듈은 유료. 무료 대안
   (`docxtemplater-image-module-free`, MIT) 사용 권장 — OK?
2. **생성 방식 A vs B**: 최종 양식에 태그 3개 넣어도 되면 (A) 추천(코드 최소).
   양식을 손대기 싫으면 (B).
3. **섹션당 이미지 수**: 1장 고정(말씀하신 의도, 추천) vs 한 섹션에 여러 장 허용?
4. **삽입 순서**: 그리드 순서(추천) vs 선택한 순서?
5. **섹션 설명 텍스트**: 비워두고 워드에서 작성(추천) vs 앱에서 사진별 캡션 입력?
6. **선택 이미지 개수 상한**: 문서당 최대 장수 제한 둘지?
7. **세션 유지**: 새로고침 시 선택 초기화 OK? (서버 없으니 기본 휘발)

## 8. 비범위 (만들지 않음)
- 서버, DB, 로그인, 저장 이력
- 이미지 편집(크롭/필터), 클라우드 연동
- 다국어, 권한 관리
